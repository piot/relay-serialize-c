# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_library(relay-serialize STATIC 
  client_in.c
  client_out.c
  debug.c
  serialize.c
  server_in.c
  server_out.c)

include(Tornado.cmake)
set_tornado(relay-serialize)

target_include_directories(relay-serialize PUBLIC ../include)


target_link_libraries(relay-serialize PUBLIC 
  flood)

